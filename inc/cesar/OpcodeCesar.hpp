#ifndef OPCODE_CESAR_HPP
#define OPCODE_CESAR_HPP

enum OpcodeCesar
{
    LOAD,
    STORE,
    ADD,
    SUB,
    JUMP,
    JEQ,
    JNE,
    JG,
    JL,
    CMP,
    CALL,
    RET,
    HALT
};

#endif
